# 認証情報。これが漏れると全録画にアクセスされる
**/.env.local
**/.env

# ビルド成果物と依存
server/node_modules/

# 打ち合わせの記録（機微情報を含むため絶対に上げない）
# 生ログには相手の経営状況・健康状態・家庭の事情がそのまま入る
meetings/
*.vtt

# OS
.DS_Store
Thumbs.db

# 販売者向けの内部資料（配布しない）
ONBOARDING.md

# ローカル専用の道具（配布しない）
# whisper2md.py = Zoomの自動文字起こしが作られなかった回に、音声から起こし直すための道具。
# 作者のマシンの絶対パスとCUDA環境に依存しているため、そのままでは他の人の環境で動かない。
# 配るなら、環境依存を外して汎用化してから。
workflow/whisper2md.py
