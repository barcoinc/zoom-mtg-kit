# Zoom MTG Kit — 導入と運用の指示

このファイルはあなた（Claude Code）への指示書です。ユーザーに読ませるものではありません。

**相手は非エンジニアだと想定してください。** 専門用語を使わず、操作は代わりにやり、
どうしても本人にやってもらう所だけを、1つずつ具体的に案内します。

---

## まず判断すること

ユーザーが最初に話しかけてきたら、`server/.env.local` があるかを確認して分岐します。

- **無い** → 「初回セットアップ」へ
- **ある** → `doctor` ツールを実行し、NGがあれば該当箇所だけ直す。全部OKなら「日常の使い方」へ

いきなり全手順を並べて説明しないでください。**1ステップずつ出して、終わったのを確認してから次に進みます。**

---

## 初回セットアップ

### 進め方の原則

- **1回のメッセージで頼むのは1つだけ。** まとめて渡すと必ずどこかで詰まります
- **あなたにできない作業がある**ことを自覚してください。ブラウザでZoomにログインして操作する部分は代われません。その時は「私にはできないので、画面を開いてこうしてください」と具体的に頼み、**終わったと言われてから次へ進みます**
- 各ステップは**必ず検証してから**次へ。「やったはず」で進めない
- 英語画面の項目名は、**画面上の英語表記をそのまま**伝える（探せなくなるため勝手に和訳しない）

### 手順0: 前提の確認

最初にこれだけ聞きます。

1. Zoomは有料プラン（Pro以上）か → **無料だとクラウド録画が使えず、この仕組みは成立しません。** ここで止めて、加入してもらう
2. そのZoomアカウントの管理者か（オーナー or 管理者）→ 一般メンバーだとアプリが作れません

`node -v` を実行してNode.js 18以上かを確認します。無ければインストールから案内します。

### 手順1: 音声文字起こしをONにする（★最頻出の失敗ポイント）

**ここを飛ばすと文字起こしが作られず、議事録が作れません。必ず最初にやらせてください。**

こう案内します。

> この設定だけは私が代わりにできないので、お願いします。
> 1. https://zoom.us/profile/setting を開いてください
> 2. 上のタブから「記録」を選びます
> 3. 「クラウド記録」をONにします
> 4. その下の「詳細クラウド記録設定」を開きます
> 5. **「音声文字起こしを作成」にチェックを入れて保存**してください
>
> チェックが入ったら教えてください。

終わったと言われたら、**「この設定を入れる前に録った分には文字起こしは作られない」**ことを伝えます。

### 手順2: Zoomアプリを作る（あなたには代われない作業）

こう案内します。一度に全部言わず、**まずアプリを作る所まで**にします。

> 1. https://marketplace.zoom.us/ にZoomでサインイン
> 2. 右上の「Develop」→「Build App」
> 3. 「**Server-to-Server OAuth**」を選んで「Create」
> 4. 名前は何でもいいので入れてください（例: MTG Kit）
>
> 作成できたら教えてください。

次に、**Information タブ**の必須項目（会社名・開発者名・メール）を埋めてもらいます。

そのあと **Scopes** を案内します。ここが一番迷う所なので、**検索して探す形**で伝えます。

> 「Scopes」タブ →「Add Scopes」を押してください。
> 検索ボックスに `meeting` と入れて、次の4つにチェックを入れます。
>
> - meeting:write:meeting:admin
> - meeting:read:meeting:admin
> - meeting:read:list_meetings:admin
> - meeting:update:meeting:admin
>
> 次に `recording` で検索して、次の5つにチェックを入れます。
>
> - cloud_recording:read:list_user_recordings:admin
> - cloud_recording:read:list_recording_files:admin
> - cloud_recording:read:meeting_transcript:admin
> - cloud_recording:update:recording_settings:admin
> - cloud_recording:delete:recording_file:admin
>
> 名前が少し違って見えることがあります。**似た名前しか無ければ、それで大丈夫**です。
> あとで自動チェックをかけるので、足りなければその時に分かります。

最後に **Activation** タブで「Activate your app」を押してもらいます。

### 手順3: 認証情報を受け取る（ここは慎重に）

> 「App Credentials」の画面に3つの値があります。
> Account ID / Client ID / Client Secret を教えてください。

受け取ったら `server/.env.local` に書き込みます。あわせて Zoom のログインメールアドレスも聞きます。

**この3つは、この `.env.local` 以外のどこにも書かないでください。** チャットへの再掲、
メモファイル、コミットログ、どれもNGです。`.gitignore` で除外済みであることも確認します。

### 手順4: 設置（ここからはあなたが全部やる）

**`npm install` も `npm run build` も不要です。** `server/dist/index.js` はビルド済みで、
必要なものは全部その1ファイルに入っています（実測確認済み）。**やらせないでください。**
Node.jsのバージョン差で失敗して、そこで止まる原因になります。

`.env.local` が無ければ `.env.local.example` からコピーして値を埋めます。

そのあとMCPサーバーとして登録します（**絶対パス**で指定）。

```bash
claude mcp add zoom-mtg-kit -- node /絶対パス/zoom-mtg-kit/server/dist/index.js
```

登録したら、**Claude Codeの再起動が必要**なことを伝えます。再起動後に `/mcp` で確認してもらいます。

### 手順5: 検証（ここまでやって初めて完了）

`doctor` ツールを実行します。NGが出たら、**その項目だけ**を直しに戻ります。
スコープ不足なら不足分を名指しで出すので、それをそのまま伝えて追加してもらい、
**Activation で再アクティベートが必要**なことも必ず添えます。

全部OKになったら、通しで動くか確認します。

1. 「テスト用のZoomリンクを作りますね」と言って `create-meeting` を実行
2. **そのリンクに3分ほど入って、何か喋って終了してもらう**
3. 文字起こしは**30〜60分かかる**ことを伝え、待つ
4. `download-recordings` を `transcriptOnly: true` で実行し、議事録まで作ってみせる

**リンクが作れた時点で「完了」と言わないでください。** 手順1がOFFのままでも
リンク作成までは正常に動いてしまいます。議事録が出るところまで見て初めて完了です。

### 手順6: 運用の設計（ここが一番価値がある）

セットアップが終わったら、そのまま使える状態にします。

1. **案件フォルダの置き場所を決める。** 既に案件ごとの管理をしているなら、そこに合わせる。
   無ければ作業フォルダの直下に作る（名前は `clients` でも `案件` でも、相手が分かる名前でよい）
2. **今抱えている案件を聞き出し、案件ごとに3点セットを作る**（`workflow/運用ルール.md` の「案件を増やす」）
   - `<案件フォルダ>/<案件名>/meetings/` を作る
   - `create-meeting` で常設リンクを発行する
   - `<案件フォルダ>/<案件名>/Zoomリンク.md` に **URL・パスコード・ミーティングIDを書く**
   - **ミーティングIDを書き忘れない。** 録画と案件を紐づける鍵で、後から復元するのが面倒になる
3. 録音用の「_未分類」も同じ3点セットで1つ作る（案件が決まっていない打ち合わせの受け皿）
4. **`ZOOM_ARCHIVE_ROOT` を `server/.env.local` に設定する。** 案件フォルダの**親**のパスを絶対パスで書く。
   これを設定すると、録画を消す前の安全装置が案件フォルダの中まで探しに行くようになる。
   **未設定だと、正しく案件フォルダへ保存した人ほど「文字起こしが無い」と誤判定される**
5. **`.gitignore` に `**/.env.local` を必ず入れる**（Zoomの認証情報。漏れると全録画にアクセスされる）。打ち合わせの記録の扱いは `workflow/運用ルール.md` に従う（プライベートリポジトリなら含めてよい／**公開に切り替える時は必ず先に外す**）
6. 発行したリンクを一覧にして渡す
7. **容量の見張りを仕掛ける。** `~/.claude/settings.json` に `SessionStart` フックを足して
   `node <キットの絶対パス>/workflow/check-capacity.mjs` を実行させる（書き方は導入マニュアルの手順10）。
   **これはあなたが代わりに書き込む。** 上限の7割を超えた時だけ警告が出る。
   ここを飛ばすと「気づいたら録画されていなかった」が必ず起きる（Proの5GBは60分×10本で埋まる）

---

## 日常の使い方

セットアップ済みのユーザーには、日本語の依頼をそのままツールに対応させます。

| 言われること | やること |
|---|---|
| 「〇〇の案件を追加して」 | **フォルダ・Zoomリンク・`Zoomリンク.md`（ID込み）の3点セット**を作る（`workflow/運用ルール.md`） |
| 「〇〇の打ち合わせ用にZoomリンク作って」 | 上と同じ。**リンクだけ作って終わりにしない。**置き場所が無いと次に迷子になる |
| 「〇〇の録画を議事録にして」 | 後述の議事録化フロー |
| 「さっきの録画の共有リンク出して」 | `get-share-link` |
| 「パスコード無しで見られるようにして」 | `set-share-settings`（`share: publicly`, `removePasscode: true`） |
| 「動かない」「おかしい」 | まず `doctor` |

### 議事録化フロー

1. `list-recordings` で対象を確認。**ファイルサイズが全部0の間はZoom側で変換中**なので待つ
2. `download-recordings` を `transcriptOnly: true` で実行（MP4は数百MBあり、議事録には不要）
3. **どの案件の録画かを、ミーティングIDで確定する。** 各案件の `Zoomリンク.md` のIDと突き合わせる。どれにも当たらなければ `_未分類/` へ
4. `workflow/vtt2md.py` でVTTを整形して `<案件名>/meetings/YYYY-MM-DD_文字起こし.md` に保存（① 丸ごと・要約しない）
5. **② 議事録と ③ 自分用メモを書く。型は `workflow/運用ルール.md` に書いてある**（そこが正本）
6. **議事録ができたら、録画を消すか必ず本人に確認する。勝手に消さない**

### 議事録を書く時の注意

- **型の正本は `workflow/運用ルール.md`。** ただし作業フォルダに `共通ルール.md` があるか、上位のルール（別のシステムに接続している等）で型が指定されている場合は、**そちらが勝つ**。迷ったら「利用者の手元にあるルール > このキットの初期値」
- **型は初期値でしかない。** 利用者が「この形を変えたい」と言ったら `workflow/運用ルール.md` を書き換える。書き換えたら以後はそれに従う
- **自動文字起こしは固有名詞をほぼ確実に間違える。** 人名・社名は「要確認」と補足に明記し、**推測で直さない**
- **話者が全員同じ名前になっている場合がある。** これは録音機として使った回（対面録音など）の正常な挙動で、壊れてはいない。`vtt2md.py` が自動判定して読める形にするので、話者は文脈から判断する。議事録の補足にその旨を書く
- **機微な内容は事実として簡潔に書き、論評しない**

---

## つまずいた時の対応表

| 症状 | 原因 | 対応 |
|---|---|---|
| `環境変数 ZOOM_... が未設定` | `.env.local` が無い/空 | 手順3-4をやり直す |
| `スコープが足りません` | Scopes の入れ忘れ | 不足名をそのまま伝えて追加 → **再アクティベート** |
| 録画はあるが文字起こしが無い | 手順1がOFFのまま録った | **その回の分は後から作れない。** 設定を入れて次回から |
| ファイルサイズが全部0 | Zoom側で変換中 | 60分の会議で30〜60分待つ |
| 配ったURLで相手が入れない | URLに `/u/4/` が混ざっている | **その部分を削って配り直す。** ブラウザのアドレスバーからコピーすると混ざる |
| 「容量がいっぱい」と言われた | クラウド録画が上限（Pro 5GB）に達した | `list-recordings` で大きい順に出し、**議事録化 → 案件フォルダの `meetings/` に保存 → 削除**の順で片付ける。削除は必ず本人に確認。片付いたら手順6の見張りが入っているか確認する |
| 共有リンクを見られないと言われた | 視聴パスコードが必要 | パスコードも一緒に伝えるか、`set-share-settings` で解除 |

---

## やってはいけないこと

- **認証情報（Account ID / Client ID / Client Secret）を `.env.local` 以外の場所に書かない。** 漏れるとその相手が全録画にアクセスできる
- **リポジトリを公開に切り替える時は、必ず先に打ち合わせの記録を外す。**打ち合わせの生ログには相手の経営状況・健康状態・家庭の事情がそのまま入る（`workflow/運用ルール.md` 参照）
- **確認なしに録画を削除しない**
- **リンクが作れただけで「セットアップ完了」と言わない**
