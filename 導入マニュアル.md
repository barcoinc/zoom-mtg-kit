# 導入マニュアル

Zoom MTG Kit を動かすまでの手順書です。**上から順にやれば20分で終わります。**

パソコンが苦手でも進められるように、画面に出てくる文字をそのまま書いています。

---

## これを入れると何が起きるか

打ち合わせが終わると、**議事録ができています。**

案件ごとにZoomのリンクを1本作っておくだけで、あとは録画も文字起こしも自動。Claude Codeに「さっきの録画を議事録にして」と言えば、決まったことと次にやることが整理されて出てきます。

---

## 0. 準備するもの

この3つが揃っていれば動きます。逆に、揃っていないと動きません。

| 必要なもの | 確認のしかた |
|---|---|
| **Zoomの有料プラン（Pro以上）** | 無料プランでは**クラウド録画が使えません**。ここだけは避けて通れません |
| **Zoomアカウントの管理者権限** | 自分が契約者（オーナー）か管理者であること。会社で招待されただけの立場だと、手順2ができません |
| **Claude Code と Node.js 18以上** | ターミナルで `node -v` と打って、v18以上の数字が出ればOK |

**プログラミングの知識は要りません。** ビルド済みのファイルが入っているので、`npm install` のような作業も不要です。

---

## 1. Zoomで「音声文字起こし」をONにする

**ここが一番大事です。飛ばさないでください。**

1. https://zoom.us/profile/setting をブラウザで開く
2. 上に並んでいるタブから **「記録」** を選ぶ
3. **「クラウド記録」** をONにする
4. その下にある **「詳細クラウド記録設定」** をクリックして開く
5. **「音声文字起こしを作成」** にチェックを入れて保存

これで、録画が終わったあとにZoomが自動で文字起こしを作ってくれます。

> **なぜ最初にやるのか**
> この設定を入れた**あとの録画からしか**文字起こしは作られません。前に録ったものに後から作ることはできません。1日遅れると、その日の打ち合わせは対象外になります。

> **どれくらい待つか**
> 会議が終わってから文字起こしができるまで、60分の会議でだいたい30〜60分かかります。すぐ出てこなくても壊れていません。

---

## 2. Zoomのアプリ（鍵）を作る

「アプリを作る」と聞くと身構えますが、やることは単純です。

**Zoomの機能を外から呼び出すための鍵を1本作って、自分のパソコンに渡すだけ。**

### 2-1. 作る

1. https://marketplace.zoom.us/ にZoomアカウントでサインイン
2. 画面右上の **Develop** → **Build App**
3. 種類を聞かれるので **「Server-to-Server OAuth」** を選んで **Create**
4. 名前を入力（何でもOK。例: `MTG Kit`）→ **Create**

> **なぜこの種類なのか**
> Server-to-Server OAuth は「自分のアカウント専用の鍵」です。他人に配るものではないので、Zoomの審査を通す必要がありません。自分の録画だけを扱う今回の用途では、これが一番早く終わります。

### 2-2. 3つの値を控える

**App Credentials** という画面に、次の3つが表示されます。あとで使うのでコピーしておきます。

- **Account ID**
- **Client ID**
- **Client Secret**

> **この3つは絶対に他人に渡さないでください。**
> 渡した相手が、あなたのZoom録画をすべて見られる状態になります。パスワードと同じ扱いにしてください。

### 2-3. Information を埋める

**Information** タブで、会社名・開発者名・連絡先メールを入力します。必須の項目だけでOKです。

---

## 3. 権限（スコープ）を選ぶ

**Scopes** タブ → **Add Scopes** を押します。

ここで項目が何百個も出てきます。初めてだと必ず迷うので、**検索して選んでください。**

### 検索ボックスに `meeting` と入れて、この4つ

```
meeting:write:meeting:admin
meeting:read:meeting:admin
meeting:read:list_meetings:admin
meeting:update:meeting:admin
```

### 続けて `recording` と入れて、この5つ

```
cloud_recording:read:list_user_recordings:admin
cloud_recording:read:list_recording_files:admin
cloud_recording:read:meeting_transcript:admin
cloud_recording:update:recording_settings:admin
cloud_recording:delete:recording_file:admin
```

### 迷いやすい2点

**末尾が `admin` のものと `master` のものが並んで出てきます。選ぶのは `admin` です。**
`master` は複数のZoomアカウントをまとめて管理する親アカウント向けなので、通常の契約では動きません。

**必要になってから足せばいい、と思わないでください。**
権限を追加するたびに有効化をやり直す必要があり、そのたびに手が止まります。最初に9個すべて入れておくほうが結果的に早く終わります。

### 有効化する

**Activation** タブ → **Activate your app** を押します。

> 名前の表記はZoomの画面のバージョンによって少し違うことがあります。似た名前しか見つからなければ、それで大丈夫です。**足りなければ後の自動チェックが教えてくれます。**

---

## 4. キットを置く

GitHubからダウンロードします。

https://github.com/barcoinc/zoom-mtg-kit

緑色の **Code** ボタン → **Download ZIP** で取得して、解凍します。置き場所はどこでも構いませんが、**あとでパスを使うので分かりやすい場所**にしてください。

解凍したら、`server` フォルダの中の `.env.local.example` をコピーして、名前を **`.env.local`** に変更します。

そのファイルをテキストエディタで開いて、**手順2-2で控えた3つ**を書き込みます。

```
ZOOM_ACCOUNT_ID=（Account ID）
ZOOM_CLIENT_ID=（Client ID）
ZOOM_CLIENT_SECRET=（Client Secret）
ZOOM_USER_EMAIL=me
```

`ZOOM_USER_EMAIL` は `me` のままで大丈夫です（自分自身を指す書き方です）。Zoomのログインメールアドレスを書いても動きます。

---

## 5. Claude Codeに登録する

ターミナルで次を実行します。**パスは、解凍した場所に置き換えてください。**

```bash
claude mcp add zoom-mtg-kit -s user -- node /置いた場所/zoom-mtg-kit/server/dist/index.js
```

実行したら、**Claude Codeを一度終了して起動し直します。** 再起動しないと読み込まれません。

`/mcp` と打って `zoom-mtg-kit` が出てくれば登録できています。

---

## 6. 動くか確かめる

Claude Codeに、日本語でそのまま話しかけます。

```
doctor を実行して
```

認証・権限・文字起こしの設定・保存先を順にチェックして、**足りないものがあれば名指しで教えてくれます。** 何か出たら、その項目だけ直してもう一度実行してください。

すべてOKになったら、通しで試します。

```
テスト用にZoomリンクを作って
```

作られたリンクに**3分ほど入って、何か喋って終了**します。

文字起こしができるまで30〜60分かかるので、その間は別の作業をしていてください。しばらくしてから、

```
さっきの録画を議事録にして
```

議事録が出てきたら完成です。

> **リンクが作れた時点で「できた」と思わないでください。**
> 手順1の文字起こしがOFFのままでも、**リンクの発行までは成功してしまいます。** そこで完了と判断すると、あとから文字起こしだけ無いことに気づきます。**議事録が出るところまで**確認してください。

---

## 7. 案件を作る（ここまでやって、やっと"仕組み"になります）

このキットの値打ちは、**打ち合わせをするたびに、お客さんごとのフォルダに記録が溜まっていくこと**です。
その入れ物を最初に作ります。Claude Codeにこう言うだけです。

```
みどり商事様の案件を追加して
```

3つがまとめて用意されます。

1. `<案件フォルダ>/みどり商事様/meetings/` … 記録が溜まっていく場所
2. その案件専用のZoomリンク（毎回同じURLを使い回せます）
3. `<案件フォルダ>/みどり商事様/Zoomリンク.md` … URL・パスコード・**ミーティングID**の控え

> **ミーティングIDが要になります。** 録画がどの案件のものかを、名前ではなくIDで突き合わせるので、
> 案件名が似ていても、あとで名前を変えても取り違えません。

案件が増えたら、そのたびに「〇〇の案件を追加して」と言えば増やせます。
案件が決まっていない打ち合わせ用に、「_未分類」も1つ作っておくと便利です
（対面の商談をスマホで録る時や、先方主催のZoomを自分側でも録る時の受け皿になります）。

### 保管場所を教えておく（1回だけ・大事）

案件フォルダを作ったら、`server/.env.local` の最後の行にその**親フォルダのパス**を書きます。

```
ZOOM_ARCHIVE_ROOT=/自分の/案件フォルダの親
```

これは**録画を消す前の安全装置**が使います。設定しておくと「この録画の文字起こしはもう保存済みか」を
案件フォルダの中まで探して確かめてくれます。**書いておかないと、きちんと整理している人ほど
「文字起こしが見つからない」と言われて削除が止まります。** 分からなければClaude Codeに任せてください。

---

## 8. 普段の使い方

日本語で頼むだけです。コマンドを覚える必要はありません。

| 言うこと | 起きること |
|---|---|
| 「〇〇社の打ち合わせ用にZoomリンク作って」 | 案件用の常設リンクを発行。URLは毎回同じなので使い回せます |
| 「Zoomの録画一覧を見せて」 | クラウドに溜まっている録画を一覧表示 |
| 「〇〇の録画を議事録にして」 | 文字起こしを取ってきて、議事録に整えて保存 |
| 「さっきの録画の共有リンク出して」 | 録画の視聴URLを取得。欠席者にそのまま渡せます |
| 「パスコード無しで見られるようにして」 | 共有設定を変更 |
| 「〇〇の録画をVimeoに上げて」 | Vimeoへ退避。URL限定公開のリンクが返ります（要トークン設定） |
| 「〇〇の録画を消して」 | ゴミ箱へ移動。**確認を取ってから実行**し、文字起こしが手元に無ければ中止します |
| 「動かない」「おかしい」 | まず `doctor` を実行してください |

議事録の型・自分用メモの型・案件フォルダの構成は、すべて **`workflow/運用ルール.md`** に書いてあります。
**これは初期値です。**「この形は違う」と思ったらそのファイルを書き換えてください（Claude Codeに言えば直します）。
以後はそちらに従って書かれます。

---

## 9. 困ったときは

まず **`doctor` を実行**してください。だいたいこれで原因が分かります。

それでも分からないときは、この表を見てください。

| 症状 | 原因と対処 |
|---|---|
| `環境変数 ZOOM_... が未設定です` | `server/.env.local` が無いか、値が空です。手順4をやり直してください |
| `Zoomアプリのスコープが足りません` | 手順3で入れ忘れています。追加したあと、**Activationで有効化をやり直す**必要があります |
| 録画はあるのに文字起こしが無い | **手順1をやっていません。** しかも**設定より前に録った分には後から作れません。** 設定を入れて、次の打ち合わせから使ってください |
| ファイルサイズが全部0バイト | Zoom側でまだ変換中です。60分の会議で30〜60分かかります |
| 参加URLを配ったら相手が入れない | URLに `/u/4/` のようなアカウント番号が混ざっています。**その部分を削ってから配ってください。** ブラウザのアドレスバーからコピーすると混ざります |
| 共有リンクを見られないと言われた | 視聴パスコードが必要です。パスコードも一緒に伝えるか、「パスコード無しで見られるようにして」と頼んでください |
| 話者が全員自分の名前になっている | 対面の打ち合わせを録音代わりに使った回では正常な挙動です。音声の入口が1つしかないためで、壊れていません |

---

## 10. 録画が容量を食ってきたら（任意）

Zoomのクラウド容量はProで5GB。60分の会議で500MB前後なので、**10本ほどで埋まります。**

消せば空きますが、**消すと共有URLも死にます。** 相手に渡したリンクが見られなくなります。

**Vimeoを契約している場合は、そこへ移せば解決します。** リンクは消えず、Zoom側は空けられます。

設定は10分です。→ **[Vimeo連携ガイド.md](Vimeo連携ガイド.md)**

> **順番だけ注意してください。** 議事録を作る → Vimeoへ退避 → Zoomを削除。
> **Zoomの録画を消すと文字起こしも消えます。** 逆順にすると議事録が作れません。
> 安全装置として、文字起こしが手元に無い状態では削除を中止します。

### 埋まる前に知らせてもらう（おすすめ）

上限に達してからでは、その日の会議が録画されません。**手前で気づける仕組み**を入れておきます。

`~/.claude/settings.json` に、次を足してください（`hooks` が既にあれば、その中に `SessionStart` を追加します）。

```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "node /絶対パス/zoom-mtg-kit/workflow/check-capacity.mjs 2>/dev/null || true",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
```

これで、Claude Codeを開いた時に使用量を確認し、**上限の7割を超えていた時だけ**「録画を整理しますか」と聞いてくるようになります。余裕がある時は何も言いません。

判定は24時間キャッシュするので、実際にZoomへ問い合わせるのは1日1回だけです。

**契約が5GBでない場合**（ライセンスを増やした、上位プランにしたなど）は、`server/.env.local` に次の1行を足してください。

```
ZOOM_STORAGE_LIMIT_GB=10
```

---

## 11. これだけは守ってください

**認証情報（Account ID / Client ID / Client Secret）を `.env.local` 以外に書かない。**
漏れると、その相手があなたの録画をすべて見られます。誰かにこのフォルダを渡すときは、`.env.local` を必ず消してください。

**議事録を共有リポジトリに上げない。**
議事録には相手の経営状況、売上、体調、家庭の事情まで入ります。共有した瞬間に全員が読める状態になります。`.gitignore` に除外設定を入れてあるので、それを外さないでください。

**録画していることは相手に伝える。**
Zoomには録画中の表示が出ますが、対面で録音代わりに使うときは自分から一言添えてください。効率化の前に、信頼のほうが大事です。
