# Vimeo連携ガイド（任意）

Zoomの録画をVimeoへ移すための設定です。**Vimeoを契約していない人は読み飛ばして構いません。**

所要10分。

---

## なぜ必要なのか

Zoomのクラウド容量は、**Proで5GB**しかありません。

60分の会議で500MB前後なので、**10本も録れば埋まります。**

空けるには消すしかない。ところが**消すと共有URLも死にます。** クライアントに「これ見ておいてください」と渡したリンクが、ある日見られなくなる。

Vimeoへ移せば、この問題が両方とも解けます。

- **Vimeo側のリンクは消えない**（容量を空けても生き続ける）
- **Zoomの5GBを毎回空けられる**
- Zoomの共有ページより**見た目が整う**（クライアントに渡すものとして成立する）

---

## 前提

**Vimeoの有料プラン**が必要です。

無料プランは週あたりのアップロード容量に制限があり、1本1GB級の会議録画だとすぐ上限に当たります。

Proの場合は4TBあるので、実質気にしなくて済みます。

---

## 手順1. アプリを作る

1. https://developer.vimeo.com/apps を開いて、Vimeoアカウントでログイン
2. **「Create an App」** を押す
3. 次を埋める

| 項目 | 入れるもの |
|---|---|
| **App name** | `Zoom MTG Kit`（何でもOK） |
| **App description** | `Zoomの録画をVimeoに退避して共有する` |
| 用途を聞かれたら | 他人に配布するアプリではないので、個人利用・自分専用の選択肢を選ぶ |

**Create** を押すと、アプリのページが開きます。

---

## 手順2. トークンを発行する ★ここで間違えやすい

アプリのページを下にスクロールすると **「Authentication」** の欄があります。

その中の **「Personal Access Tokens」** → **「Generate an access token」** を押します。

### スコープ（権限）を必ず選ぶ

チェックボックスが並んでいます。**次の4つを必ず入れてください。**

```
Upload
Edit
Private
Video Files
```

`Public` は既定で入っていることが多いですが、それだけでは**アップロードできません。**

> **実際にやった失敗**
> スコープを選ばずに発行したところ、`public` だけのトークンができました。
> 認証は通るのにアップロードだけ失敗するので、原因が分かりにくい状態になります。
> **チェックを入れたことを目視で確認してから発行してください。**

### 発行したらすぐコピーする

生成されたトークンが表示されます。

**この画面を離れると二度と表示されません。** その場でコピーしてください。

失敗したら、同じ手順で作り直せます（古いほうは削除しておくと安全です）。

---

## 手順3. 設定ファイルに書く

`server/.env.local` をテキストエディタで開いて、末尾に1行足します。

```
VIMEO_ACCESS_TOKEN=（コピーしたトークン）
```

これで完了です。Claude Codeを再起動してください。

> **このトークンも他人に渡さないでください。** 渡した相手が、あなたのVimeoに動画を上げたり消したりできます。
> `.gitignore` で除外されているので、リポジトリには上がりません。

---

## 手順4. 使ってみる

Claude Codeにこう言います。

```
〇〇の録画をVimeoに上げて
```

こう返ってきます。

```
Vimeoへの取り込みを開始しました
対象: たなか先生 勉強会（555MB）
リンク: https://vimeo.com/1214225102/xxxxxxxx
公開範囲: unlisted
状態: in_progress
```

**この時点ではまだ視聴できません。** Vimeo側で変換が走っています。

500MBで10〜30分が目安です。待ってから確認します。

```
Vimeoの状態を確認して
```

`available` になれば、そのURLをそのまま渡せます。

> **手元の回線は使いません。**
> VimeoがZoomから直接ファイルを取得する方式（pull）を使っています。1GBの動画をダウンロードしてアップロードし直す、という往復が発生しません。

---

## 公開範囲の選び方

既定は **URL限定公開（unlisted）** です。URLを知っている人だけが見られて、Vimeo上の検索には出ません。

変えたい場合は言葉で頼めます。

| 頼み方 | 設定 | 向いている場面 |
|---|---|---|
| （何も言わない） | **URL限定公開** | クライアントに渡す。ほとんどの場合これで足ります |
| 「パスワードをかけて」 | パスワード必須 | 有料コンテンツ、社外に出したくない録画 |
| 「自分だけ見られるようにして」 | 非公開 | 保管だけが目的のとき |
| 「ダウンロードも許可して」 | DL可 | 相手に手元保存してほしいとき |

### 埋め込みについて

**埋め込みは既定で「どこでも可」にしています。** これは意図的です。

Vimeoにはドメインを指定して埋め込みを制限する設定がありますが、**登録し忘れたドメインでは「この動画は再生できません」と表示されます。** LPを公開した直後に気づく、という事故が起きやすい設定です。

URL限定公開なら、**そもそもURLを知らない人は埋め込めません。** 埋め込み制限は追加の防御になりません。

会員サイト、LP、ProLineのページなど、いろいろな場所に貼る運用なら**既定のままが安全**です。

---

## 順番を守ってください ★重要

```
① 議事録を作る  →  ② Vimeoへ退避  →  ③ Zoomの録画を削除
```

**この順を逆にすると議事録が作れません。**

理由は、**Zoomの録画を消すと文字起こしも一緒に消える**からです。Vimeoに動画が残っていても、文字起こしはZoom側にしかありません。

安全装置を入れてあります。

- **文字起こしが手元に無い状態では、削除を中止します**
- 削除は**確認を取ってからでないと実行しません**

「消していい？」と聞かれたら、議事録ができているか確かめてから答えてください。

---

## 困ったとき

| 症状 | 原因と対処 |
|---|---|
| `VIMEO_ACCESS_TOKEN が未設定です` | 手順3をやり直してください。書いたあとの再起動も必要です |
| アップロードだけ失敗する | **トークンのスコープに `Upload` が入っていません。** 手順2から作り直してください |
| `Upload access` の申請を求められた | Vimeo側で申請が必要なアカウントです。フォームを送って承認を待ってください（数日かかることがあります） |
| いつまでも `in_progress` のまま | Zoomの署名付きURLには有効期限があります。時間が経ちすぎている場合は、もう一度アップロードし直してください |
| 相手が「見られない」と言う | 公開範囲がパスワード付きか非公開になっている可能性があります。「状態を確認して」と頼めば現在の設定が出ます |

---

## 容量を確認したいとき

Vimeoの残り容量が気になったら、こう聞けます。

```
Vimeoの残り容量を確認して
```

プランと、使用済み・残りのストレージが出ます。
